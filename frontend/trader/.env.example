# =============================================================
# TrustEdge Trader Frontend — .env.example
# Copy to .env.local for local development
# =============================================================

# ─── Server-side only (NOT exposed to browser) ─────────────────
# Where Next.js server proxies /api/v1/* requests to the gateway.
# Docker default: http://gateway:8000 (set in docker-compose.yml).
# Local dev: uncomment below.
# GATEWAY_INTERNAL_URL=http://127.0.0.1:8000

# ─── NEXT_PUBLIC_* — CAUTION: baked into JS bundle at build time ─
# On HTTPS sites, NEVER set these to http:// or ws:// — browsers block mixed content.
# Leave UNSET in production: the app auto-detects protocol.
#   • API calls → same-origin /api/v1 (proxied server-side to gateway)
#   • WebSocket → wss://<current host>/ws/prices (Nginx must proxy /ws/ to gateway)
#
# Only set for local dev (HTTP):
# NEXT_PUBLIC_WS_URL=ws://localhost:8000
