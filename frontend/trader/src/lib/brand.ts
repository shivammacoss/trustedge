/** Dashboard / in-app product name (landing & marketing stay ProTrader). */
export const BRAND_NAME = 'TrustEdge';

/** Zustand persist key for UI preferences (theme, terminal layout). */
export const STORAGE_KEY_UI = 'trustedge-ui';

/** Legacy key — migrated once on load; logout clears legacy auth key. */
export const STORAGE_KEY_UI_LEGACY = 'protrader-ui';
